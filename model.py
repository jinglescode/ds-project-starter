def create_model():
    return

def train_model(model, X, Y):
    # fit the model with train X and Y
    # you might want to change the input params to `train_X, train_Y, val_X, val_Y`
    return

def test_model(model, X, Y):
    # test the model with test data
    # check error rate of actual Y and predicted Y
    # maybe you want to return accuracy/RMSE?
    return
